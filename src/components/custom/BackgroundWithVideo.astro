---

const { url } = Astro.props;

---

<video autoplay muted loop style="width:100%" class="bg-gray-700">
  <source src={url} type="video/mp4">
  Your browser does not support the video tag.
</video>