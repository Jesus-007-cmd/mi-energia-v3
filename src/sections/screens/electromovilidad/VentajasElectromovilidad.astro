---
const ventajasList = [
  'Poder reservar el punto de carga a través de la app',
  'Administrar tarifas de carga dependiendo del día y del horario',
  'Llevar un control de los cobros, cargas y clientes',
  'Establecer tarifas de ocupación de espacio.',
];

---
<div class="px-6 pt-12 md:pb-14 md:px-20 lg:px-40 xl:px-60 flex flex-col bg-gray-100 text-center items-center gap-6 sm:pb-8 sm:mb-8">

  <!-- Título -->
  <h3 class="font-semibold text-2xl">
    Ventajas de ser anfitrión de una estación de carga
  </h3>

  <!-- Contenedor principal -->
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6 items-center w-full">

    <!-- Imagen -->
    <div class="w-full h-auto flex justify-center items-center">
      <img 
        src="/images/screens/electromovilidad/img25.jpg" 
        class="rounded w-full h-[40vh] md:h-[50vh] lg:h-[60vh] object-contain"
      />
    </div>

    <!-- Lista de ventajas -->
    <div class="grid grid-cols-1 gap-4 sm:gap-6 md:grid-cols-2 lg:grid-cols-2 w-full">
      {ventajasList.map((row, index) => (
        <div class="border rounded p-4 flex flex-col gap-4 bg-white items-center">
          <div class="h-12 w-12 border rounded flex justify-center items-center bg-gray-100">
            <span class="text-blue-700 font-semibold">{index + 1}</span>
          </div>
          <h2>{row}</h2>
        </div>
      ))}
    </div>

  </div>
<div class="sm:pb-8 sm:mb-8"></div>

</div>
