---
const backgroundImage = {
  backgroundImage: 'url(/images/screens/monta-cargas/img2.jpg)',
  backgroundSize: 'cover', // Asegura que la imagen cubra el contenedor
  backgroundPosition: 'center', // Centra la imagen
  backgroundRepeat: 'no-repeat', // Evita la repetición
};

const info = `
  Optimizar la operación de tus vehículos analizando el volumen de uso y el tiempo de operación.
  Mi Energía MX establece criterios claros con los operadores 
  para mejorar las prácticas de carga y reducir el Costo Total de Propiedad (TCO).`;
---

<div class="grid grid-cols-1 md:grid-cols-2 h-auto md:h-96">

  <!-- Contenedor de texto -->
  <div class="bg-neutral-200 p-6 flex flex-col justify-center gap-4 text-black">
    <p class="text-2xl md:text-3xl font-semibold leading-snug">
      Hasta el 90% del consumo de energía eléctrica de un CEDIS puede provenir de su sala de carga.
    </p>
    <p class="text-sm md:text-base">{info}</p>
  </div>

  <!-- Contenedor de imagen -->
  <div 
    style={backgroundImage} 
    class="h-64 md:h-auto w-full bg-cover bg-center"
  ></div>

</div>
